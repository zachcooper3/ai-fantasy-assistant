# Fantasy Football Draft Assistant — Python dependencies
# Install with: pip install -r requirements.txt

# API framework
fastapi>=0.115.0
uvicorn>=0.46.0

# Database
sqlmodel>=0.0.21

# Vector store (RAG layer) — now wired into ai_service.py's recommendation
# prompt (see backend/rag/vector_store.py). Re-commented out on 2026-07-25
# was temporary, while the layer was dead code; it's live again as of the
# player-news/analytics RAG build.
chromadb>=1.5.0

# NFL stats (opportunity/volume, efficiency, team context, consistency/risk,
# forward-looking signals — see backend/ingestion/fetch_metrics.py). This is
# nflreadpy, NOT the deprecated nfl_data_py — the nflverse project retired
# nfl_data_py in favor of this in 2026.
nflreadpy>=0.1.5

# AI
anthropic>=0.102.0

# HTTP client (Sleeper API calls, RotoWire RSS)
httpx>=0.28.0

# Config / settings
pydantic>=2.13.0
pydantic-settings>=2.14.0

# Loads .env into the process environment (see backend/__init__.py) — every
# entrypoint in this app reads config via os.getenv(), and nothing else was
# actually reading the .env file before this.
python-dotenv>=1.0.1
